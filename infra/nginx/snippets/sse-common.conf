# Server-Sent Events tuning: stream immediately, never time the connection out.
# Deployed to /etc/nginx/snippets/sse-common.conf

proxy_buffering     off;
proxy_cache         off;
proxy_read_timeout  24h;
proxy_send_timeout  24h;
gzip                off;
chunked_transfer_encoding off;
